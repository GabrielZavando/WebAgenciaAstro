---
interface Props {
  code?: string;
  message?: string;
  emoji?: string;
  buttonText?: string;
  buttonHref?: string;
}
const {
  code = "404",
  message = "Oops... La página que buscas no existe o ha sido movida.",
  emoji = "🚧",
  buttonText = "Volver al inicio",
  buttonHref = "/"
} = Astro.props;
---

<section style="min-height:60vh;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;">
  <div style="font-size:3rem;margin-bottom:1rem;">{emoji}</div>
  <h1 style="font-size:6rem;margin-bottom:0.5rem;color:#556;">{code}</h1>
  <p style="font-size:1.2rem;margin-bottom:1.5rem;">{message}</p>
  <a href={buttonHref} style="display:inline-block;padding:0.75rem 1.5rem;background-color:#0077cc;color:white;text-decoration:none;border-radius:5px;font-weight:bold;transition:background-color 0.3s ease;">{buttonText}</a>
</section>
