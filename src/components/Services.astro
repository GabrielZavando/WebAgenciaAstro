---
// Importamos los datos de servicios (después los crearemos)
const services = [
  {
    id: 1,
    icon: "🌐",
    title: "Servicio 1",
    description: "Descripción corta del servicio 1",
    modalContent: "Contenido detallado del servicio 1"
  },
  {
    id: 2,
    icon: "💻",
    title: "Servicio 2",
    description: "Descripción corta del servicio 2",
    modalContent: "Contenido detallado del servicio 2"
  },
  {
    id: 3,
    icon: "📱",
    title: "Servicio 3",
    description: "Descripción corta del servicio 3",
    modalContent: "Contenido detallado del servicio 3"
  }
];
---

<section id="servicios" class="services">
  <h2>Servicios</h2>
  <p class="services-description">
    Descubre cómo puedo ayudarte a transformar tu negocio con soluciones digitales personalizadas. Desde el desarrollo web hasta la implementación de chatbots con inteligencia artificial, mis servicios están diseñados para optimizar tus procesos y ayudarte a alcanzar tus objetivos."
  </p>
  
  <div class="services-grid">
    {services.map(service => (
      <div class="service-card">
        <div class="icon">{service.icon}</div>
        <h3>{service.title}</h3>
        <p>{service.description}</p>
        <button class="modal-trigger" data-modal={`modal-${service.id}`}>
          Ver más
        </button>
      </div>
    ))}
  </div>

  {/* Modales */}
  {services.map(service => (
    <div class="modal" id={`modal-${service.id}`}>
      <div class="modal-content">
        <button class="modal-close">&times;</button>
        <h3>{service.title}</h3>
        <div class="modal-body">
          {service.modalContent}
        </div>
      </div>
    </div>
  ))}
</section>

<script>
  // Importamos y ejecutamos la inicialización de modales
  import { initializeModals } from '../scripts/modal';
  initializeModals();
</script>

<style lang="scss">
  .services {
    padding: 4rem 2rem;
    text-align: center;

    h2 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      position: relative;
      display: inline-block;
      
      &::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 50%;
        transform: translateX(-50%);
        width: 100px;
        height: 3px;
        background-color: #007bff;
      }
    }

    &-description {
      max-width: 800px;
      margin: 0 auto 3rem;
      line-height: 1.6;
    }

    &-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }
  }

  .service-card {
    padding: 2rem;
    background: white;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease;

    &:hover {
      transform: translateY(-5px);
    }

    .icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    h3 {
      margin-bottom: 1rem;
    }

    button {
      margin-top: 1rem;
      padding: 0.5rem 1.5rem;
      border: none;
      background-color: #007bff;
      color: white;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;

      &:hover {
        background-color: #0056b3;
      }
    }
  }

  .modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5);
    z-index: 1000;
    
    &.active {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    &-content {
      background: white;
      padding: 2rem;
      border-radius: 10px;
      width: 90%;
      height: 90%;
      max-width: 800px;
      position: relative;
      overflow-y: auto;
    }

    &-close {
      position: absolute;
      top: 1rem;
      right: 1rem;
      font-size: 2rem;
      background: none;
      border: none;
      cursor: pointer;
      padding: 0.5rem;
      line-height: 1;
    }

    &-body {
      margin-top: 2rem;
    }
  }
</style>